{
  "name": "chatty-server",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "local": "./node_modules/.bin/nodemon --ignore public/ -L",
    "start": "./node_modules/.bin/nodemon --ignore public/ -L",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "4.16.3",
    "uuid": "^3.3.2",
    "ws": "6.0.0"
  },
  "devDependencies": {
    "nodemon": "^1.18.3"
  }
}
